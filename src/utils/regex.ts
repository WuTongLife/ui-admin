const imgRegex = /\.(img|png|svg|jpg|jpeg|gif)$/i;
const videoRegex = /\.(mp4)$/i;
const pdfRegex = /\.(pdf)$/i;
const wordRegex = /\.(doc|docx)$/i;
const excelRegex = /\.(xls|xlsx)$/i;

export { imgRegex, videoRegex, pdfRegex, wordRegex, excelRegex };
